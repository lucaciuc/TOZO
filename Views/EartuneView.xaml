<UserControl x:Class="TozoWindowsApp.Views.EartuneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="420"
             Background="#222327">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="16,24,16,16">
            <Button Command="{Binding CloseViewCommand}" 
                    Style="{StaticResource MaterialDesignToolButton}" 
                    HorizontalAlignment="Left" 
                    Foreground="White">
                <materialDesign:PackIcon Kind="ChevronLeft" Width="24" Height="24"/>
            </Button>
            <TextBlock Text="Eartune" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16,0,16,16">
                <!-- EQ Graph Placeholder -->
                <materialDesign:Card UniformCornerRadius="12" Background="#2C2F36" Padding="16" Height="200" Margin="0,0,0,24">
                    <Grid>
                        <TextBlock Text="Frequency Response" Foreground="#888888" FontWeight="SemiBold" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <!-- Fake Sliders for EQ -->
                        <UniformGrid Columns="10" Margin="0,30,0,0">
                            <!-- Just visual mocks for a premium feel -->
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="2" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="5" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="1" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="-2" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="-1" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="0" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="3" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="4" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="2" Foreground="#4A90E2" Margin="2,0"/>
                            <Slider Orientation="Vertical" Minimum="-10" Maximum="10" Value="1" Foreground="#4A90E2" Margin="2,0"/>
                        </UniformGrid>
                    </Grid>
                </materialDesign:Card>

                <TextBlock Text="Presets" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,16"/>

                <UniformGrid Columns="2">
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Standard" Foreground="White" BorderBrush="#444444" Margin="0,0,8,12" Height="45"/>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Bass +" Foreground="White" BorderBrush="#4A90E2" Background="#2C3A5A" Margin="8,0,0,12" Height="45"/>
                    
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Bass -" Foreground="White" BorderBrush="#444444" Margin="0,0,8,12" Height="45"/>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Treble +" Foreground="White" BorderBrush="#444444" Margin="8,0,0,12" Height="45"/>
                    
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Treble -" Foreground="White" BorderBrush="#444444" Margin="0,0,8,12" Height="45"/>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Vocal" Foreground="White" BorderBrush="#444444" Margin="8,0,0,12" Height="45"/>
                </UniformGrid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
