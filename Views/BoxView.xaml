<UserControl x:Class="TozoWindowsApp.Views.BoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="420"
             Background="#222327">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="16,24,16,16">
            <Button Command="{Binding CloseViewCommand}" 
                    Style="{StaticResource MaterialDesignToolButton}" 
                    HorizontalAlignment="Left" 
                    Foreground="White">
                <materialDesign:PackIcon Kind="ChevronLeft" Width="24" Height="24"/>
            </Button>
            <TextBlock Text="BOX" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <!-- Box Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16,0,16,16">
                
                <!-- Image Placeholder -->
                <Grid Height="200" Margin="0,16,0,16">
                    <materialDesign:PackIcon Kind="BatteryCharging100" Width="100" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#444444"/>
                </Grid>

                <!-- Battery -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,32">
                    <TextBlock Text="{Binding CaseBatteryText}" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ProgressBar Value="{Binding CaseBattery}" Visibility="{Binding IsCaseBatteryKnown, Converter={StaticResource BoolToVis}}" Minimum="0" Maximum="100" Height="4" Width="60" VerticalAlignment="Center" Foreground="White" Background="#444444"/>
                </StackPanel>

                <!-- Wallpaper Card -->
                <materialDesign:Card UniformCornerRadius="12" Background="#2C2F36" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <TextBlock Text="Wallpaper" Foreground="White" FontWeight="Bold" HorizontalAlignment="Left"/>
                            <TextBlock Text="Custom wallpaper >" Foreground="#888888" HorizontalAlignment="Right"/>
                        </Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Border Width="110" Height="60" CornerRadius="8" Background="#4A26AB" Margin="0,0,8,0"/>
                            <Border Width="110" Height="60" CornerRadius="8" Background="#FF5722" Margin="0,0,8,0"/>
                            <Border Width="110" Height="60" CornerRadius="8" Background="#2196F3"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Settings Card -->
                <materialDesign:Card UniformCornerRadius="12" Background="#2C2F36" Padding="16" Margin="0,0,0,32">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <TextBlock Text="Screen Brightness" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="Low >" Foreground="#888888" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,16">
                            <TextBlock Text="Screen On Time" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="10s >" Foreground="#888888" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,16">
                            <TextBlock Text="Language" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="English >" Foreground="#888888" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,16">
                            <TextBlock Text="Time Display" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="24H >" Foreground="#888888" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="OTA Upgrade" Foreground="White" FontWeight="Bold"/>
                            <TextBlock Text="Current V1.4.1 >" Foreground="#888888" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>

                <Button Content="Disconnect Charging Case" 
                        Style="{StaticResource MaterialDesignFlatButton}" 
                        Foreground="#FF5252" FontWeight="Bold" FontSize="14"
                        HorizontalAlignment="Center" Margin="0,0,0,32"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
